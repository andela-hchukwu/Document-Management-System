language: node_js
node_js:
    - "stable"

services:
    - postgresql
 
script:
    - npm test
before_script:
    - npm install
    - psql -c 'create user andeladeveloper;'
    - psql -c 'create database documents_test;' -U andeladeveloper


after_script: cat coverage/*lcov.info | ./node_modules/.bin/coveralls 