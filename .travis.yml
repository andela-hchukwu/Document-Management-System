language: node_js
node_js:
    - "stable"

services:
    - postgresql
 
script:
    - npm test
before_script:
    - npm install
    - psql -c 'create database documents-dev;' -U postgres


after_script: cat coverage/*lcov.info | ./node_modules/.bin/coveralls 